Recognize fridge products from image and return JSON only.
Return all product names and units in Russian (Cyrillic), suitable for direct UI rendering.
Always return confidence for every product as a number from 0.0 to 1.0.
If no food products are visible, return {{"recognized_products": []}}.

Client context (device local date/time, for seasonal or freshness wording if needed):
{client_context}

Return format:
{{
  "recognized_products": [
    {{"name": "string", "amount": 0, "unit": "string", "confidence": 0.0}}
  ]
}}

Existing products (for merge/append logic):
{existing_products}

